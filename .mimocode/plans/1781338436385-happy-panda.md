# Improve Cat Guide — per-page dialogs, faster typing, more animations

## Changes

### 1. guideData.ts — add page-specific dialogs
Add dialogs for: roadmaps, profile, lessons, mentors, quiz. Each with unique RU/EN messages and character states.

### 2. GuideCharacter.tsx — page detection + more animations
- Detect current page via `currentPage` from LanguageContext
- Show page-specific dialog when entering a new page
- Add behaviors: sleep after 30s inactivity, wake on hover, head bop to music, tail wag faster on hover
- Remove `hidden md:block` to show on mobile too

### 3. GuideDialog.tsx — speed up typing
- Typing speed: 30ms → 12ms per character
- Auto-advance delay: 3000ms → 1500ms

## Files to modify
1. `src/app/components/utils/guideData.ts`
2. `src/app/components/utils/GuideCharacter.tsx`
3. `src/app/components/utils/GuideDialog.tsx`
